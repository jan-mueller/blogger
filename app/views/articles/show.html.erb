<h1><%= @article.title %></h1>

<p>
  Tags:
  <% @article.tags.each do |tag| %>
    <%= link_to tag.name, tag_path(tag) %>
  <% end %>
</p>


<p><%= @article.body %></p>

<h3>Kommentare (<%= @article.comments.size %>)</h3>
<%= render partial: 'articles/comment', collection: @article.comments %>

<%= render partial: 'comments/form' %>

<%= link_to "<< Zurück zur Artikel Übersicht", articles_path %>

<%= link_to "Löschen", article_path(@article), method: :delete, data: {confirm: "Diesen Artikel wirklich löschen?"} %>

<%= link_to "Bearbeiten", edit_article_path(@article)%>